config_version: 1
project:
  name: TransitEye
reproducibility:
  master_seed: 42
evaluation:
  policy_version: grouped-robustness-domain-shift-v2
  model_version: model-4a58f313bc50b9c3dc41
  # Fixed-threshold LOTO avoids unstable nested threshold fitting with only six groups.
  development_resampling: leave_one_tic_out_fixed_threshold
  fixed_threshold: 0.325
  bootstrap_replicates: 1000
  confidence_level: 0.95
  bootstrap_seed_component: grouped_metric_bootstrap
  # This is the already-frozen, predeclared D001--D008 grid; it is not regenerated here.
  injection_grid_identity: demo-manifest-9459aec7c2e26e406301
  feature_ablation_groups: [time_domain, bls, lomb_scargle, fft]
  missing_feature_stress_groups: [lomb_scargle, fft]
  # Symmetric feature-input stress only; it does not alter detector candidates.
  candidate_period_perturbation_fraction: 0.005
  robust_support_iqr_multiplier: 1.5
  error_high_missingness_count: 10
